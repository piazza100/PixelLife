app:
  cors:
    allowed-origin: ${FRONTEND_ORIGIN}
  frontend-url: ${FRONTEND_URL}

billing:
  polar:
    # Launch mode: production web/API with Polar Sandbox. Change Render env values together before real sales.
    api-base: ${POLAR_API_BASE:https://sandbox-api.polar.sh/v1}

server:
  servlet:
    session:
      cookie:
        secure: true
        same-site: none
