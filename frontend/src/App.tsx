import { useEffect, useMemo, useRef, useState } from 'react'
import { ApiError, authLinks, pixelLifeApi, type Member, type RewardData, type RewardPlant } from './api'

type Locale = 'en' | 'ko' | 'zh' | 'ja'
type InputType = 'level' | 'check' | 'mood'
type Entry = { date: string; value: number; note?: string; emoji?: string }
type Board = { id: string; title: string; inputType: InputType; startDate: string; goalDays: number | null; entries: Entry[]; color: string; status?:'ACTIVE'|'COMPLETED' }
type View = 'home' | 'detail' | 'setup' | 'active-list' | 'finished-list' | 'garden-list' | 'conservatory-list' | 'guide' | 'rewards' | 'account' | 'privacy' | 'terms'

const words = {
  en:{hello:'GOOD TO SEE YOU',headline:'Your small things\nare adding up.',sub:'Pick a board and add today.',new:'＋ New board',active:'active',archived:'Finished boards',archiveHelp:'Your finished boards stay here.',day:'DAY',ready:'Ready to start',missed:'days missed · Start again today',done:'days done',wins:'wins',plus:'Plus is on',plusSub:'Up to 30 active boards',myBoards:'My boards',back:'← My boards',newBoard:'NEW BOARD',question:'What do you want\nto keep doing?',name:'Board name',period:'Goal length',endless:'Endless',make:'Make board',cancel:'Cancel',board:'BOARD',rolling:'13 WEEK BOARD',first:'Today can be your first pixel.',again:'One win still counts. Begin again.',moving:'The board moves with today. Your history stays safe.',today:'TODAY',how:'How was today?',change:'You can change it any time.',pick:'Pick 1 to 5. There is no bad score.',note:'Short note',optional:'optional',placeholder:'One small thing',save:'Save today',saved:'Saved ✓',daysDone:'Days done',daysMissed:'Days missed',keep:'Keep rate',boardTab:'Board',stats:'Stats',older:'Older board',newer:'Newer board',scroll:'Swipe or use the buttons',noteTitle:'Note on',noNote:'No note for this day.',weekRate:'This week',average:'Avg. level',best:'Best week',milestones:'Current streak',reached:'active',next:'next',local:'Dates use your local time.'},
  ko:{hello:'다시 만나 반가워요',headline:'작은 일들이\n차곡차곡 쌓이고 있어요.',sub:'보드를 골라 오늘을 기록하세요.',new:'＋ 새 보드',active:'진행 중',archived:'종료된 보드',archiveHelp:'끝난 보드는 여기에서 다시 볼 수 있어요.',day:'일차',ready:'시작할 준비가 됐어요',missed:'일 놓침 · 오늘 다시 시작해요',done:'일 기록',wins:'회 기록',plus:'Plus 이용 중',plusSub:'활성 보드 최대 30개',myBoards:'내 보드',back:'← 내 보드',newBoard:'새 보드',question:'무엇을 꾸준히\n하고 싶나요?',name:'보드 이름',period:'목표 기간',endless:'무기한',make:'보드 만들기',cancel:'취소',board:'보드',rolling:'13주 보드',first:'오늘 첫 픽셀을 만들 수 있어요.',again:'한 번의 성공도 소중해요. 오늘 다시 시작해요.',moving:'오늘을 기준으로 보드가 이동하며 기록은 보존돼요.',today:'오늘',how:'오늘은 어땠나요?',change:'언제든 오늘 기록을 바꿀 수 있어요.',pick:'1부터 5까지 골라요. 나쁜 점수는 없어요.',note:'짧은 메모',optional:'선택',placeholder:'오늘의 작은 일',save:'오늘 저장',saved:'저장됨 ✓',daysDone:'기록한 날',daysMissed:'놓친 날',keep:'기록률',boardTab:'보드',stats:'통계',older:'이전 보드',newer:'다음 보드',scroll:'스와이프하거나 버튼으로 이동',noteTitle:'이날의 메모',noNote:'이날 작성한 메모가 없어요.',weekRate:'이번 주',average:'평균 단계',best:'최고의 주',milestones:'현재 연속 기록',reached:'적용 중',next:'다음',local:'사용자 현지 날짜를 기준으로 해요.'},
  zh:{hello:'很高兴再次见到你',headline:'每一件小事\n都在慢慢累积。',sub:'选择一个面板，记录今天。',new:'＋ 新面板',active:'进行中',archived:'已结束面板',archiveHelp:'结束的面板会保存在这里。',day:'第',ready:'准备开始',missed:'天未记录 · 今天重新开始',done:'天已记录',wins:'次记录',plus:'Plus 已启用',plusSub:'最多30个活动面板',myBoards:'我的面板',back:'← 我的面板',newBoard:'新面板',question:'你想坚持做\n什么？',name:'面板名称',period:'目标天数',endless:'无限',make:'创建面板',cancel:'取消',board:'面板',rolling:'13周面板',first:'今天可以点亮第一个像素。',again:'一次成功也很重要。今天重新开始。',moving:'面板随今天移动，历史记录会保留。',today:'今天',how:'今天怎么样？',change:'你可以随时修改今天。',pick:'选择1到5，没有坏分数。',note:'简短笔记',optional:'可选',placeholder:'今天的一件小事',save:'保存今天',saved:'已保存 ✓',daysDone:'记录天数',daysMissed:'未记录',keep:'完成率',boardTab:'面板',stats:'统计',older:'更早面板',newer:'更新面板',scroll:'滚动或滑动查看更多',noteTitle:'当天笔记',noNote:'这一天没有笔记。',weekRate:'本周',average:'平均等级',best:'最佳一周',milestones:'里程碑',reached:'已达成',next:'下一个',local:'日期以你的本地时间为准。'},
  ja:{hello:'また会えてうれしいです',headline:'小さなことが\n積み重なっています。',sub:'ボードを選んで今日を記録しましょう。',new:'＋ 新しいボード',active:'進行中',archived:'終了したボード',archiveHelp:'終了したボードはここで見返せます。',day:'日目',ready:'始める準備ができました',missed:'日未記録 · 今日から再開',done:'日記録',wins:'回記録',plus:'Plus 利用中',plusSub:'アクティブボード最大30個',myBoards:'マイボード',back:'← マイボード',newBoard:'新しいボード',question:'何を続けたい\nですか？',name:'ボード名',period:'目標期間',endless:'無期限',make:'ボードを作る',cancel:'キャンセル',board:'ボード',rolling:'13週間ボード',first:'今日、最初のピクセルを作れます。',again:'一度の成功も大切です。今日から再開。',moving:'今日を基準にボードが動き、履歴は残ります。',today:'今日',how:'今日はどうでしたか？',change:'今日の記録はいつでも変更できます。',pick:'1から5を選びます。悪い点数はありません。',note:'短いメモ',optional:'任意',placeholder:'今日の小さなこと',save:'今日を保存',saved:'保存しました ✓',daysDone:'記録した日',daysMissed:'未記録日',keep:'記録率',boardTab:'ボード',stats:'統計',older:'前のボード',newer:'次のボード',scroll:'スクロールまたはスワイプ',noteTitle:'この日のメモ',noNote:'この日のメモはありません。',weekRate:'今週',average:'平均レベル',best:'最高の週',milestones:'達成記録',reached:'達成',next:'次',local:'日付は端末の現地時間が基準です。'}
} as const

const pad=(n:number)=>String(n).padStart(2,'0')
const day=(offset=0)=>{const d=new Date();d.setHours(12,0,0,0);d.setDate(d.getDate()+offset);return d}
const key=(d:Date)=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`
const today=key(day())
const BOARD_PAGE_DAYS=35
const diff=(a:string,b:string)=>Math.floor((new Date(`${b}T12:00:00`).getTime()-new Date(`${a}T12:00:00`).getTime())/86400000)
const endDate=(board:Board)=>board.goalDays?key((()=>{const d=new Date(`${board.startDate}T12:00:00`);d.setDate(d.getDate()+board.goalDays-1);return d})()):null
const isFinished=(board:Board)=>board.status==='COMPLETED'
const titleCase=(value:string)=>value.toLowerCase().split('_').map(word=>word.charAt(0).toUpperCase()+word.slice(1)).join(' ')
const GUEST_BOARD_KEY='pixellife-guest-board-v1'
const loadGuestBoards=():Board[]=>{try{const value=localStorage.getItem(GUEST_BOARD_KEY);const saved=value?JSON.parse(value):[];return Array.isArray(saved)?saved:(saved?[saved]:[])}catch{return[]}}
const emptyBoard:Board={id:'',title:'',inputType:'level',startDate:today,goalDays:null,entries:[],color:'#159651',status:'ACTIVE'}
const fromApiBoard=(b:any,entries:any[]=[]):Board=>({id:String(b.id),title:b.name,inputType:b.boardType==='CHECK'?'check':b.boardType==='MOOD'?'mood':'level',startDate:b.startDate,goalDays:b.goalDays,entries:entries.map(e=>({date:e.entryDate,value:e.numericValue??(e.success?1:0),note:e.note||undefined,emoji:e.emoji||undefined})),color:b.color,status:b.status})

function App(){
  const[locale,setLocale]=useState<Locale>('en');const progress={en:'IN PROGRESS',ko:'진행 중',zh:'进行中',ja:'進行中'}[locale];const t={...words[locale],rolling:progress}
  const[member,setMember]=useState<Member|null|undefined>(undefined)
  const[rewards,setRewards]=useState<RewardData|null>(null)
  const[busy,setBusy]=useState(false);const[notice,setNotice]=useState('')
  const[boards,setBoards]=useState<Board[]>(loadGuestBoards);const[selected,setSelected]=useState('');const[view,setView]=useState<View>('home');const importAsked=useRef(false)
  useEffect(()=>{let alive=true;const load=async()=>{const guest=loadGuestBoards();try{const account=await pixelLifeApi.me(locale);let data=await pixelLifeApi.bootstrap(locale);if(!importAsked.current&&guest.some(b=>!isFinished(b))&&!data.boards.some(b=>b.status==='ACTIVE')){importAsked.current=true;const source=guest.find(b=>!isFinished(b))!;if(confirm(`Save your guest board “${source.title}” to this account?`)){await pixelLifeApi.importGuestBoard({name:source.title,type:source.inputType==='check'?'CHECK':source.inputType==='mood'?'MOOD':'LEVEL',startDate:source.startDate,goalDays:source.goalDays,entries:source.entries.map(e=>({date:e.date,value:source.inputType==='level'?e.value:undefined,success:source.inputType==='check'?true:undefined,emoji:source.inputType==='mood'?e.emoji:undefined,note:e.note}))});localStorage.removeItem(GUEST_BOARD_KEY);data=await pixelLifeApi.bootstrap(locale)}}const loaded=await Promise.all(data.boards.map(async b=>{const detail=await pixelLifeApi.getBoard(b.id);return fromApiBoard(detail.board,detail.entries)}));if(!alive)return;setMember(account);setRewards(data.rewards);setBoards(loaded);setSelected(v=>loaded.some(b=>b.id===v)?v:(loaded[0]?.id||''))}catch{if(!alive)return;setMember(null);setRewards(null);setBoards(guest);setSelected(guest[0]?.id||'')}};load();return()=>{alive=false}},[locale])
  useEffect(()=>{if(member===null)localStorage.setItem(GUEST_BOARD_KEY,JSON.stringify(boards))},[boards,member])
  const[title,setTitle]=useState('Move my body');const[goal,setGoal]=useState<number|null>(30);const[customGoal,setCustomGoal]=useState(45);const[inputType,setInputType]=useState<InputType>('level');const[level,setLevel]=useState(3);const[mood,setMood]=useState('😊');const[note,setNote]=useState('');const[saved,setSaved]=useState(false)
  const board=boards.find(b=>b.id===selected)||boards[0]||emptyBoard
  const showError=(error:unknown,fallback:string)=>{if(error instanceof ApiError&&error.status===401){setNotice('Your session ended. Please sign in again.');setMember(null)}else setNotice(error instanceof Error?error.message:fallback)}
  ;(t as any).resetToday=async()=>{if(busy)return;setBusy(true);setNotice('');try{if(member)await pixelLifeApi.resetToday(Number(board.id),today);setBoards(v=>v.map(b=>b.id===board.id?{...b,entries:b.entries.filter(e=>e.date!==today)}:b));setNote('');setSaved(false)}catch(error){showError(error,'Could not reset today')}finally{setBusy(false)}}
  ;(t as any).hasToday=board.entries.some(e=>e.date===today)
  const open=(id:string)=>{setSelected(id);setView('detail');setNote('');setSaved(false)}
  const add=async()=>{if(busy)return;if(!member&&boards.some(b=>!isFinished(b))){setNotice('Guest mode allows one active board. Sign in or finish this board first.');return}setBusy(true);setNotice('');try{const days=goal===-1?Math.max(1,customGoal):goal;if(member){const created=await pixelLifeApi.createBoard({name:title.trim()||'My goal',type:inputType==='check'?'CHECK':inputType==='mood'?'MOOD':'LEVEL',startDate:today,goalDays:days});const next=fromApiBoard(created);setBoards(v=>[next,...v]);setSelected(next.id)}else{const id=`guest-${Date.now()}`;const next:Board={id,title:title.trim()||'My goal',inputType,startDate:today,goalDays:days,entries:[],color:'#159651',status:'ACTIVE'};setBoards(v=>[next,...v]);setSelected(id)}setView('detail')}catch(error){showError(error,'Could not make this board')}finally{setBusy(false)}}
  const save=async()=>{if(busy)return;setBusy(true);setNotice('');try{const entry={date:today,value:board.inputType==='level'?level:1,note:note.trim()||undefined,emoji:board.inputType==='mood'?mood:undefined};if(member)await pixelLifeApi.saveEntry(Number(board.id),today,board.inputType==='level'?{value:level,note:entry.note}:board.inputType==='check'?{success:true,note:entry.note}:{emoji:mood,note:entry.note});setBoards(v=>v.map(b=>b.id===board.id?{...b,entries:[...b.entries.filter(e=>e.date!==today),entry]}:b));setSaved(true);setTimeout(()=>setSaved(false),1300)}catch(error){showError(error,'Could not save today')}finally{setBusy(false)}}
  const finish=async()=>{if(busy||!confirm('Finish this board? You cannot add more records after this.'))return;setBusy(true);setNotice('');try{if(member){await pixelLifeApi.completeBoard(Number(board.id));const [nextRewards,nextMember]=await Promise.all([pixelLifeApi.rewards(),pixelLifeApi.me(locale)]);setRewards(nextRewards);setMember(nextMember)}setBoards(v=>v.map(b=>b.id===board.id?{...b,status:'COMPLETED'}:b));setView('home')}catch(error){showError(error,'Could not finish this board')}finally{setBusy(false)}}
  const upgrade=async()=>{if(!member){location.href=authLinks.google;return}if(busy)return;setBusy(true);setNotice('');try{const checkout=await pixelLifeApi.createPlusCheckout();location.href=checkout.url}catch(error){showError(error,'Checkout is not ready yet');setBusy(false)}}
  const active=boards.filter(b=>!isFinished(b)), archived=boards.filter(isFinished);const plants=member?rewards?.plants||[]:[];const writable=(b:Board)=>!member||member.effectivePlan==='PLUS'||member.writableBoardId===Number(b.id)
  if(member===undefined)return <div className="app app-loading" aria-busy="true"><div><span>P</span><b>PixelLife</b><p>Loading your garden…</p></div></div>
  return <div className={`app ${busy?'is-busy':''}`} aria-busy={busy}><header><button className="brand" onClick={()=>setView('home')}><span>P</span>PixelLife</button><div className="plan"><nav className="help-nav"><button onClick={()=>setView('guide')}>Guide</button><button onClick={()=>setView('rewards')}>Rewards</button></nav><label className="language"><span>◎</span><select aria-label="Language" value={locale} onChange={e=>setLocale(e.target.value as Locale)}><option value="en">🇺🇸 English</option><option value="ko">🇰🇷 한국어</option><option value="zh">🇨🇳 中文</option><option value="ja">🇯🇵 日本語</option></select></label>{member?<><b>{member.effectivePlan}</b><button className="account-link" onClick={()=>setView('account')}>Account</button></>:<a className="login-button" href={authLinks.google}>Sign in with Google</a>}</div></header>{notice&&<div className="notice" role="alert"><span>{notice}</span><button aria-label="Close message" onClick={()=>setNotice('')}>×</button></div>}
  {view==='home'&&<main className="home"><section className="intro"><div><p className="eyebrow">{t.hello}</p><h1>{t.headline.split('\n').map((x,i)=><span key={x}>{x}{i===0&&<br/>}</span>)}</h1><p>{t.sub}</p></div><button className="new-button" onClick={()=>setView('setup')}>{t.new}</button></section><section className="section-head"><div><h2>{t.myBoards}</h2><span>{active.length} {t.active}</span></div>{active.length>4&&<button onClick={()=>setView('active-list')}>All active boards ({active.length}) →</button>}</section><section className="cards">{active.slice(0,4).map(b=><BoardCard key={b.id} board={b} t={t} locked={!writable(b)} onOpen={()=>open(b.id)}/>)}</section><Garden boards={active} plants={plants} onGrowing={()=>setView('garden-list')} onComplete={()=>setView('conservatory-list')}/>{archived.length>0&&<section className="archive"><div className="section-head"><div><h2>{t.archived}</h2><span>{archived.length} complete</span></div><button onClick={()=>setView('finished-list')}>All finished boards ({archived.length}) →</button></div><p>Completed boards move their plants to the Conservatory.</p><div className="archive-list">{archived.slice(0,3).map(b=><button key={b.id} onClick={()=>open(b.id)}><span>{b.inputType==='level'?'1–5':b.inputType==='check'?'✓':'☺'}</span><b>{b.title}</b><small>{b.startDate} — {endDate(b)}</small></button>)}</div></section>}<section className="plus-summary"><div><span className="spark">✦</span><div><b>{member?.effectivePlan==='PLUS'?t.plus:'Need more boards?'}</b><p>{t.plusSub}</p></div></div>{member?.effectivePlan!=='PLUS'&&<button onClick={upgrade}>Try Plus</button>}</section></main>}
  {(view==='active-list'||view==='finished-list')&&<main className={`board-list-page ${view}`}><button className="back" onClick={()=>setView('home')}>← Home</button><section className="list-title"><p className="eyebrow">{view==='active-list'?'GROWING':'COMPLETED'}</p><div className="list-heading"><h1>{view==='active-list'?t.myBoards:t.archived}</h1><b>{view==='active-list'?active.length:archived.length} boards</b></div><p>{view==='active-list'?'Boards you can record today.':'All finished boards are below. They are read-only and their plants stay in the Conservatory.'}</p></section><section className="cards">{(view==='active-list'?active:archived).map(b=><BoardCard key={b.id} board={b} t={t} locked={!isFinished(b)&&!writable(b)} onOpen={()=>open(b.id)}/>)}</section></main>}
  {(view==='garden-list'||view==='conservatory-list')&&<main className="board-list-page"><button className="back" onClick={()=>setView('home')}>← Home</button><section className="list-title"><p className="eyebrow">{view==='garden-list'?'PIXEL GARDEN':'CONSERVATORY'}</p><h1>{view==='garden-list'?'Growing plants':'My plant collection'}</h1><p>{view==='garden-list'?'A new board always appears first.':'Every completed board becomes a permanent specimen.'}</p></section><section className={`plant-collection ${view==='conservatory-list'?'completed':''}`}>{view==='garden-list'?active.map(b=><Plant key={b.id} board={b}/>):plants.map(p=><CollectedPlant key={p.id} plant={p}/>)}</section></main>}
  {view==='guide'&&<GuidePage onBack={()=>setView('home')} onStart={()=>setView('setup')} onRewards={()=>setView('rewards')}/>} 
  {view==='rewards'&&(member?<RewardsPage rewards={rewards} onBack={()=>setView('home')}/>:<main className="guide-page"><button className="back" onClick={()=>setView('home')}>← Home</button><section className="guide-hero"><p className="eyebrow">MEMBER REWARDS</p><h1>Keep your garden<br/>after you leave.</h1><p>Sign in to earn plants, badges, and grades. Guest records stay only in this browser.</p><a className="login-button" href={authLinks.google}>Sign in with Google</a></section></main>)} 
  {view==='account'&&member&&<AccountPage member={member} onBack={()=>setView('home')} onPortal={async()=>{if(busy)return;setBusy(true);try{location.href=(await pixelLifeApi.createCustomerPortal()).url}catch(error){showError(error,'Could not open billing')}finally{setBusy(false)}}} onDelete={async()=>{if(!confirm('Delete your account and every board, plant, badge, and record? This cannot be undone.'))return;if(!confirm('Are you sure? This is the final check.'))return;setBusy(true);try{await pixelLifeApi.deleteAccount();localStorage.removeItem(GUEST_BOARD_KEY);location.href=authLinks.logout}catch(error){showError(error,'Could not delete account');setBusy(false)}}}/>} 
  {view==='privacy'&&<LegalPage title="Privacy" onBack={()=>setView('home')} paragraphs={['PixelLife stores your Google account ID, email, display name, boards, daily records, rewards, and subscription state so the service can work.','Payment details are handled by Polar. PixelLife does not store full card numbers.','You can delete your account from Account. Deletion removes your PixelLife boards, records, plants, and badges. Operational billing event records may be retained only when required for security or legal duties.']}/>} 
  {view==='terms'&&<LegalPage title="Terms" onBack={()=>setView('home')} paragraphs={['PixelLife is a simple personal tracking tool. It is not medical, financial, or professional advice.','Free allows one active board. Plus allows up to 30 active boards while the paid period is valid. Existing data stays available after Plus ends.','You may cancel Plus in the billing portal. Access continues until the end of the paid period.']}/>} 
  {view==='setup'&&<main className="setup"><button className="back" onClick={()=>setView('home')}>{t.back}</button><section className="setup-card"><p className="eyebrow">{t.newBoard}</p><h1>{t.question.split('\n').map((x,i)=><span key={x}>{x}{i===0&&<br/>}</span>)}</h1><label><span>{t.name}</span><input value={title} onChange={e=>setTitle(e.target.value)} maxLength={48}/></label><fieldset><legend>Record type</legend>{([{id:'level',icon:'1–5',name:'Level',help:'Pick how strong today was'},{id:'check',icon:'✓',name:'Yes / No',help:'One simple check each day'},{id:'mood',icon:'☺',name:'Mood',help:'Pick one emoji each day'}] as const).map(v=><button key={v.id} className={inputType===v.id?'selected':''} onClick={()=>setInputType(v.id)}><b>{v.icon}</b><span><strong>{v.name}</strong><small>{v.help}</small></span><i>{inputType===v.id?'●':'○'}</i></button>)}</fieldset><fieldset><legend>{t.period}</legend>{[30,60,90,null,-1].map(v=><button key={String(v)} className={goal===v?'selected':''} onClick={()=>setGoal(v)}><b>{v===null?'∞':v===-1?'✎':v}</b><span><strong>{v===null?t.endless:v===-1?'Custom':`${v} days`}</strong><small>{v===null?t.moving:v===-1?'Enter your own number':key(day(v-1))}</small></span><i>{goal===v?'●':'○'}</i></button>)}</fieldset>{goal===-1&&<label><span>Custom days</span><input type="number" min="1" max="3650" value={customGoal} onChange={e=>setCustomGoal(Math.max(1,Number(e.target.value)))}/></label>}<div className="actions"><button onClick={()=>setView('home')}>{t.cancel}</button><button className="primary" onClick={add}>{t.make}</button></div></section></main>}
  {view==='detail'&&board.id&&<Detail board={board} t={t} locale={locale} level={level} setLevel={setLevel} mood={mood} setMood={setMood} note={note} setNote={setNote} saved={saved} locked={!writable(board)} onUpgrade={upgrade} onSave={save} onFinish={finish} onBack={()=>setView('home')}/>}<footer><b>PixelLife</b><span>Small days. Big life.</span><nav><button onClick={()=>setView('privacy')}>Privacy</button><button onClick={()=>setView('terms')}>Terms</button></nav></footer>{busy&&<div className="busy-bar" aria-hidden="true"/>}</div>
}

function AccountPage({member,onBack,onPortal,onDelete}:{member:Member;onBack:()=>void;onPortal:()=>void;onDelete:()=>void}){return <main className="guide-page account-page"><button className="back" onClick={onBack}>← Home</button><section><p className="eyebrow">ACCOUNT</p><h1>{member.displayName}</h1><p>{member.email}</p><div className="account-plan"><span>Current plan</span><b>{member.effectivePlan}</b><small>{member.activeBoardLimit} active board{member.activeBoardLimit>1?'s':''}</small></div><button className="primary" onClick={onPortal}>Manage billing</button><a href={authLinks.logout}>Log out</a></section><section className="danger-zone"><h2>Delete account</h2><p>All boards, records, plants, badges, and XP will be permanently removed. Active Plus must be canceled and expired first.</p><button onClick={onDelete}>Delete my account</button></section></main>}
function LegalPage({title,paragraphs,onBack}:{title:string;paragraphs:string[];onBack:()=>void}){return <main className="guide-page legal-page"><button className="back" onClick={onBack}>← Home</button><article><p className="eyebrow">PIXELLIFE</p><h1>{title}</h1><p className="legal-date">Draft · August 20, 2026</p>{paragraphs.map(p=><p key={p}>{p}</p>)}</article></main>}

function GuidePage({onBack,onStart,onRewards}:{onBack:()=>void;onStart:()=>void;onRewards:()=>void}){const steps=[['1','Make a board','Write one small goal. Pick 1–5, Yes / No, or Mood.'],['2','Add today','One tap makes one pixel. A short note is optional.'],['3','See your pattern','Open Stats to see your rate and your record type chart.'],['4','Grow a plant','Every board grows one plant from its own records.'],['5','Finish and collect','A finished board becomes read-only. Its plant moves to the Conservatory.']];return <main className="guide-page"><button className="back" onClick={onBack}>← Home</button><section className="guide-hero"><p className="eyebrow">HOW PIXELLIFE WORKS</p><h1>Small days become<br/>a life you can see.</h1><p>PixelLife is a simple daily board. Add one small record, grow a clear pattern, and collect the plant you earned.</p><button onClick={onStart}>Make my first board →</button></section><section className="guide-steps">{steps.map(([n,title,copy])=><article key={n}><span>{n}</span><div><h2>{title}</h2><p>{copy}</p></div></article>)}</section><section className="guide-types"><div><b>1–5</b><h3>Level</h3><p>Pick how strong today was.</p></div><div><b>✓</b><h3>Yes / No</h3><p>Use one simple daily check.</p></div><div><b>☺</b><h3>Mood</h3><p>Save one emoji for today.</p></div></section><button className="rewards-link" onClick={onRewards}>See plants and badges →</button></main>}

function RewardsPage({onBack,rewards}:{onBack:()=>void;rewards:RewardData|null}){if(!rewards)return <main className="guide-page rewards-page"><button className="back" onClick={onBack}>← Home</button><p>Loading rewards…</p></main>;return <main className="guide-page rewards-page"><button className="back" onClick={onBack}>← Home</button><section className="guide-hero reward-hero"><p className="eyebrow">{rewards.gradeCode} · {rewards.totalXp} XP</p><h1>Finish boards.<br/>Grow your reward pool.</h1><p>Your final board score becomes XP. Grades unlock plant species. Badges unlock plant colors.</p></section><section className="grade-catalog">{rewards.gradeGuide.map((item,i)=><article className={item.code===rewards.gradeCode?'current':''} key={item.code}><span>{item.species}</span><h2>{titleCase(item.code)}</h2><b>{item.xp.toLocaleString()} XP</b><p>{item.species} plants in the random pool</p></article>)}</section><section className="rare-rule"><span>✦</span><div><h2>Your current draw</h2><p>{rewards.speciesPool.map(item=>`${item.symbol} ${item.name} ${item.chance}%`).join(' · ')}</p><div className="reward-colors">{rewards.unlockedColors.map(color=><i key={color.code} title={titleCase(color.code)} style={{background:color.cssColor}}/>)}</div></div></section><section className="badge-guide"><div><p className="eyebrow">COLOR BADGES</p><h2>{rewards.badges.filter(b=>Boolean(b.earned)).length} of {rewards.badges.length} earned.</h2><p>Each badge adds one plant color to future board rewards.</p></div><div className="badge-grid">{rewards.badges.map(badge=><article className={badge.earned?'earned':'locked'} key={badge.code}><b>{badge.earned?'✓':'○'}</b><div><h3>{badge.name}</h3><p>{badge.description}</p><span>{Math.min(badge.currentValue,badge.targetValue)} / {badge.targetValue} · {titleCase(badge.unlockColor)}</span></div></article>)}</div></section></main>}

function BoardCard({board,t,onOpen,locked=false}:{board:Board;t:any;onOpen:()=>void;locked?:boolean}){const days=diff(board.startDate,today)+1,wins=board.entries.length;const recent=Array.from({length:14},(_,i)=>board.entries.find(e=>e.date===key(day(i-13))));return <button className={`board-card ${locked?'locked':''}`} onClick={onOpen} style={{'--board-color':board.color} as React.CSSProperties}><div className="card-top"><span className="type-icon">{board.inputType==='level'?'1–5':board.inputType==='check'?'✓':'☺'}</span><span>{locked?'Read only':'↗'}</span></div><div><small>{t.day} {days}</small><h3>{board.title}</h3><p>{days===1?t.ready:wins===1?`${days-wins} ${t.missed}`:`${wins} ${t.done}`}</p><small className="board-dates">{board.startDate} → {endDate(board)||'∞'}</small></div><div className="card-pixels">{recent.map((e,i)=><i key={i} className={e?`done level-${board.inputType==='level'?e.value:5}`:''}>{board.inputType==='mood'?e?.emoji:''}</i>)}</div><div className="card-foot"><span>{wins} {t.wins}</span><b>{Math.round(wins/days*100)}%</b></div></button>}

function Plant({board,complete=false}:{board:Board;complete?:boolean}){const wins=board.entries.length;const stage=wins>=90?5:wins>=30?4:wins>=7?3:wins>=3?2:1;const species=board.inputType==='level'?'Growth tree':board.inputType==='check'?'Steady cactus':'Mood flower';const mark=board.inputType==='level'?(stage>=4?'♣':'♧'):board.inputType==='check'?'♜':'✿';return <div className={`plant stage-${stage} species-${board.inputType} ${complete?'complete':''}`} style={{'--plant':board.color} as React.CSSProperties}><div className="plant-art"><i/><b>{stage===1?'·':mark}</b>{complete&&<em>✓</em>}</div><span className="species">{species}</span><strong>{board.title}</strong><small>{complete?`${wins} records · Completed ${endDate(board)}`:`${wins} records · Stage ${stage}`}</small></div>}
function CollectedPlant({plant}:{plant:RewardPlant}){return <div className="plant complete" style={{'--plant':plant.cssColor} as React.CSSProperties}><div className="plant-art"><i/><b>{plant.symbol}</b><em>✓</em></div><span className="species">{plant.speciesName}</span><strong>{plant.boardName}</strong><small>{plant.score} points · {plant.earnedAt?.slice(0,10)}</small></div>}
function Garden({boards,plants,onGrowing,onComplete}:{boards:Board[];plants:RewardPlant[];onGrowing:()=>void;onComplete:()=>void}){const[mode,setMode]=useState<'list'|'map'>('map');const[zoom,setZoom]=useState(.8);const[pos,setPos]=useState({x:0,y:0});const drag=useRef({x:0,y:0,px:0,py:0});return <section className="garden"><div className="garden-head"><div><p className="eyebrow">PIXEL GARDEN</p><h2>Your boards grow here.</h2><p>One board grows one plant. New plants come first.</p></div><button onClick={onGrowing}>All growing plants ({boards.length}) →</button></div><div className="garden-plants">{boards.slice(0,4).map(board=><Plant key={board.id} board={board}/>)}</div><div className="conservatory"><div className="garden-head"><div><p className="eyebrow">CONSERVATORY</p><h2>All completed plants.</h2><p>These are the real plants earned from finished boards.</p></div><button onClick={onComplete}>Open full collection →</button></div><div className="garden-tabs"><button className={mode==='list'?'active':''} onClick={()=>setMode('list')}>List</button><button className={mode==='map'?'active':''} onClick={()=>setMode('map')}>Map</button><span>{plants.length} plants</span></div>{plants.length===0?<p className="empty-garden">Finish a board to collect your first plant.</p>:mode==='list'?<div className="conservatory-list">{plants.map(plant=><CollectedPlant key={plant.id} plant={plant}/>)}</div>:<div className="garden-map-shell"><div className="garden-map" onPointerDown={e=>{drag.current={x:e.clientX,y:e.clientY,px:pos.x,py:pos.y};e.currentTarget.setPointerCapture(e.pointerId)}} onPointerMove={e=>{if(e.currentTarget.hasPointerCapture(e.pointerId))setPos({x:drag.current.px+e.clientX-drag.current.x,y:drag.current.py+e.clientY-drag.current.y})}}><div className="garden-map-world" style={{transform:`translate(${pos.x}px,${pos.y}px) scale(${zoom})`}}>{plants.map(plant=><button key={plant.id} style={{'--plant':plant.cssColor,left:`${38+plant.mapX*92}px`,top:`${35+plant.mapY*92}px`} as React.CSSProperties} title={`${plant.boardName} · ${plant.score} points`}><b>{plant.symbol}</b><small>{plant.boardName}</small></button>)}</div></div><div className="map-controls"><button onClick={()=>setZoom(v=>Math.max(.45,v-.15))}>−</button><button onClick={()=>{setZoom(.8);setPos({x:0,y:0})}}>Fit</button><button onClick={()=>setZoom(v=>Math.min(1.6,v+.15))}>＋</button></div></div>}</div></section>}

function Detail({board,t,locale,level,setLevel,mood,setMood,note,setNote,saved,locked,onUpgrade,onSave,onFinish,onBack}:{board:Board;t:any;locale:Locale;level:number;setLevel:(n:number)=>void;mood:string;setMood:(s:string)=>void;note:string;setNote:(s:string)=>void;saved:boolean;locked:boolean;onUpgrade:()=>void;onSave:()=>void;onFinish:()=>void;onBack:()=>void}){
  t.inputType=board.inputType;t.board=board;t.mood=mood;t.setMood=setMood
  const[page,setPage]=useState(0);const[tab,setTab]=useState<'board'|'stats'>('board');const[loading,setLoading]=useState(false);const[inspect,setInspect]=useState<Entry|undefined>(board.entries.filter(e=>e.note).at(-1));const swipeStart=useRef(0)
  const days=diff(board.startDate,today)+1,wins=board.entries.length,avg=wins?board.entries.reduce((a,e)=>a+e.value,0)/wins:0
  const anchor=endDate(board)&&isFinished(board)?endDate(board)!:today
  const maxPage=Math.floor(Math.max(0,diff(board.startDate,anchor))/BOARD_PAGE_DAYS)
  const changePage=(direction:number)=>{const next=Math.min(maxPage,Math.max(0,page+direction));if(next===page)return;setLoading(true);setTimeout(()=>{setPage(next);setLoading(false)},220)}
  const calendar=useMemo(()=>{const last=new Date(`${anchor}T12:00:00`);last.setDate(last.getDate()+(6-last.getDay())-(page*BOARD_PAGE_DAYS));const first=new Date(last);first.setDate(last.getDate()-(BOARD_PAGE_DAYS-1));const createdWeek=new Date(`${board.startDate}T12:00:00`);createdWeek.setDate(createdWeek.getDate()-createdWeek.getDay());const visibleFirst=first<createdWeek?createdWeek:first;const count=Math.floor((last.getTime()-visibleFirst.getTime())/86400000)+1;return Array.from({length:Math.max(7,count)},(_,i)=>{const d=new Date(visibleFirst);d.setDate(visibleFirst.getDate()+i);const k=key(d),entry=board.entries.find(e=>e.date===k);return{k,num:board.inputType==='mood'&&entry?.emoji?entry.emoji:d.getDate(),entry,available:k>=board.startDate&&k<=anchor}})},[board,page,anchor])
  const streakStartOffset=board.entries.some(e=>e.date===today)?0:1
  let currentStreak=0;while(board.entries.some(e=>e.date===key(day(-(streakStartOffset+currentStreak)))))currentStreak++
  const streakDates=new Set(Array.from({length:currentStreak},(_,i)=>key(day(-(streakStartOffset+i)))))
  const milestones=[3,7,10,30];const next=milestones.find(n=>currentStreak<n)
  const streakTier=[30,10,7,3].find(n=>currentStreak>=n)
  const weeks=Array.from({length:13},(_,w)=>Array.from({length:7},(_,d)=>board.entries.some(e=>e.date===key(day(-((12-w)*7+(6-d)))))).filter(Boolean).length);const best=Math.max(...weeks)
  const dateLocale={en:'en-US',ko:'ko-KR',zh:'zh-CN',ja:'ja-JP'}[locale]
  return <main className="detail" style={{'--board-color':board.color} as React.CSSProperties}><button className="back" onClick={onBack}>{t.back}</button><nav className="detail-tabs"><button className={tab==='board'?'active':''} onClick={()=>setTab('board')}>{t.boardTab}</button><button className={tab==='stats'?'active':''} onClick={()=>setTab('stats')}>{t.stats}</button></nav>{tab==='board'?<div className="detail-grid"><section className={`calendar-card ${loading?'loading':''}`} onPointerDown={e=>{swipeStart.current=e.clientY}} onPointerUp={e=>{const delta=e.clientY-swipeStart.current;if(Math.abs(delta)>45)changePage(delta>0?1:-1)}}><div className="detail-head"><div><p className="eyebrow">{t.day} {days} · {t.rolling}</p><h1>{board.title}</h1><p>{wins===0?t.first:wins===1?t.again:t.moving}</p></div><span className="range">{calendar[0].k}<br/>— {calendar.at(-1)?.k}</span></div>{page<maxPage&&<button className="load-edge" onClick={()=>changePage(1)}>↑ {t.older}</button>}<div className="weekdays">{Array.from({length:7},(_,i)=>new Intl.DateTimeFormat(dateLocale,{weekday:'short'}).format(new Date(2026,7,16+i))).map(x=><span key={x}>{x}</span>)}</div><div className="calendar-grid">{calendar.map(c=>{const streakClass=c.entry&&streakDates.has(c.k)&&streakTier?`streak s${streakTier}`:'';return <button key={c.k} disabled={!c.available} onClick={()=>setInspect(c.entry)} className={`${c.entry?`done level-${c.entry.value}`:''} ${c.k===today?'today':''} ${c.entry?.note?'has-note':''} ${streakClass}`} title={c.k}><small>{c.num}</small>{c.entry?.note&&<i className="note-dot"/>}</button>})}</div>{page>0&&<button className="load-edge bottom" onClick={()=>changePage(-1)}>↓ {t.newer}</button>}<p className="scroll-tip">↕ {t.scroll} · {t.local}</p><div className="calendar-key">{[1,2,3,4,5].map(n=><span key={n}><i className={`level-${n}`}/>{n}</span>)}</div>{inspect&&<div className="shown-note"><span>✎</span><div><b>{t.noteTitle} {new Intl.DateTimeFormat(dateLocale,{month:'short',day:'numeric'}).format(new Date(`${inspect.date}T12:00:00`))}</b><p>{inspect.note||t.noNote}</p></div></div>}</section><aside className="side">{locked?<section className="readonly-card"><p className="eyebrow">READ ONLY</p><h2>Your records are safe.</h2><p>Free keeps one active board open. Start Plus again to record on this board.</p><button onClick={onUpgrade}>Start Plus again</button></section>:<Today t={t} level={level} setLevel={setLevel} note={note} setNote={setNote} saved={saved} onSave={onSave}/>} {!isFinished(board)&&!locked&&<button className="finish-board" onClick={onFinish}>Finish board</button>}<section className="milestone-box"><p className="eyebrow">{t.milestones} · {currentStreak}</p><div>{milestones.map(n=><span className={currentStreak>=n?`hit m${n}`:''} key={n}><b>{n}</b><small>{currentStreak>=n?t.reached:t.next}</small></span>)}</div>{next&&<p>{next-currentStreak} {t.next}</p>}</section></aside></div>:<Stats t={t} wins={wins} days={days} avg={avg} best={best} weeks={weeks}/>}</main>
}

function Today({t,level,setLevel,note,setNote,saved,onSave}:{t:any;level:number;setLevel:(n:number)=>void;note:string;setNote:(s:string)=>void;saved:boolean;onSave:()=>void}){if(isFinished(t.board))return null;return <section className="today-card"><p className="eyebrow">{t.today}</p><h2>{t.inputType==='check'?'Did you do it?':t.inputType==='mood'?'How do you feel?':t.how}</h2><p>{t.inputType==='check'?'One tap is enough.':t.inputType==='mood'?'Pick one mood for today.':t.pick}</p>{t.inputType==='level'&&<div className="levels">{[1,2,3,4,5].map(n=><button key={n} className={`level-${n} ${level===n?'selected':''}`} onClick={()=>setLevel(n)}><b>{n}</b><span>{['Try','Light','Good','Strong','Best'][n-1]}</span></button>)}</div>}{t.inputType==='check'&&<button className="check-today" onClick={onSave}>✓ Yes, I did</button>}{t.inputType==='mood'&&<div className="mood-today">{['😄','😊','😐','😔','😴'].map(x=><button key={x} className={t.mood===x?'selected':''} onClick={()=>t.setMood(x)}>{x}</button>)}</div>}<label><span>{t.note} <small>{t.optional}</small></span><input value={note} onChange={e=>setNote(e.target.value)} placeholder={t.placeholder}/></label>{t.inputType!=='check'&&<button className="save" onClick={onSave}>{saved?t.saved:t.save}</button>}{t.hasToday&&<button className="reset-today" onClick={t.resetToday}>Reset today</button>}</section>}
function Stats({t,wins,days,avg,weeks}:{t:any;wins:number;days:number;avg:number;best:number;weeks:number[]}){const board=t.board as Board;const rate=Math.round(wins/days*100);const recent=weeks.slice(-4);const recentDone=recent.reduce((a,n)=>a+n,0);const levelCounts=[1,2,3,4,5].map(n=>board.entries.filter(e=>e.value===n).length);const moods=['😄','😊','😐','😔','😴'];return <section className="stats-page friendly"><div className="stats-welcome"><div><p className="eyebrow">{t.stats}</p><h1>{rate>=70?'You are doing well.':rate>=35?'Every return counts.':'Today is a good restart.'}</h1><p>You recorded <b>{recentDone}</b> times in the last 4 weeks.</p></div><div className="rate-ring" style={{'--rate':`${rate*3.6}deg`} as React.CSSProperties}><strong>{rate}%</strong><span>{t.keep}</span></div></div><div className="type-stats"><div className="chart-head"><div><h2>{board.inputType==='level'?'Your 1–5 levels':board.inputType==='check'?'Done and missed days':'Your saved moods'}</h2><p>{board.inputType==='level'?'How often you picked each color.':board.inputType==='check'?'A simple view of your check-ins.':'The moods you chose on recorded days.'}</p></div></div>{board.inputType==='level'&&<div className="level-distribution">{levelCounts.map((count,i)=><div key={i}><i className={`level-${i+1}`} style={{height:`${Math.max(8,count/Math.max(...levelCounts,1)*100)}%`}}/><b>{count}</b><span>{i+1}</span></div>)}</div>}{board.inputType==='check'&&<div className="check-distribution"><div style={{'--part':`${rate}%`} as React.CSSProperties}/><p><b>{wins}</b> done <span>·</span> {Math.max(0,days-wins)} not recorded</p></div>}{board.inputType==='mood'&&<div className="mood-distribution">{moods.map(x=><div key={x}><b>{x}</b><span>{board.entries.filter(e=>e.emoji===x).length}</span></div>)}</div>}</div></section>}
export default App
